# Build a Football Player Card Builder

Build a FIFA-style football player card builder using React and TypeScript syntax extension (TSX), the TypeScript equivalent of JSX.

## Technologies

Uses HTML5, CSS3 and TypeScript.
